# Moved

Active numbered task documents now live in the repository-root **`Tasks/`** folder
(the user's requested canonical location). See `../../Tasks/README.md` for the index.
The historical `docs/Tasks/Task.md` (Task 004) was removed upstream after completion;
its outcome is recorded in `docs/DECISIONS.md`, `docs/TEST_REPORT.md` and `docs/BETA_RELEASE.md`.
